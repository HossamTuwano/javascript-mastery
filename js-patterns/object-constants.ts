// there are no constants in js
// for convention sake it is advised to use uppercase
// when naming variable that won't change

// constructor
var Widget = function () {
  // implementation
  var MAX_WIDTH = function () {}
  var MAX_WIDTH = function () {}
}

// constants
;(Widget.MAX_WIDTH = 320), (Widget.MAX_HEIGHT = 480)
